package com.dormrepair.file.service; import com.dormrepair.common.enums.FileBizTypeEnum;import com.dormrepair.file.vo.*;import org.springframework.web.multipart.MultipartFile;import java.util.List;
public interface FileService {FileUploadResponse upload(MultipartFile file,FileBizTypeEnum bizType);void deleteUnusedFile(Long fileId);void bindOrderFiles(Long orderId,List<Long> fileIds,Long userId);void bindProcessFiles(Long processId,List<Long> fileIds,Long userId);void bindReworkFiles(Long reworkId,List<Long> fileIds,Long userId);List<FileResponse> listOrderFiles(Long orderId);List<FileResponse> listProcessFiles(Long processId);List<FileResponse> listReworkFiles(Long reworkId);int cleanupExpiredOrphans(int limit);}
