# 前端页面与路由映射

本文对应 `FRONTEND-PLAN.md` 的 F0-01，作为 HTML 原型还原到 Vue 前端工程的页面基线。原型只用于确认视觉、布局与交互，不作为运行时数据源；正式页面不得读取 `prototype/assets/data.js` 或浏览器本地演示状态。

## 1. 路由原则

- 登录与注册使用公共布局；注册是登录页内的表单切换，不单独建立业务入口。
- 学生端和维修人员端使用移动端布局，管理员端使用桌面侧边栏布局。
- 工单详情统一使用后端工单主键 `:id`，页面展示的工单编号不是路由主键。
- `student-repair-create.html` 仅保留为原型兼容入口；正式功能是学生首页内的提交报修弹窗。
- `admin-settings.html` 不在已确认导航中，不纳入首期 Vue 路由。

## 2. 公共页面

| HTML 原型 | Vue 路由 | 页面组件 | 入口与跳转 | 公共能力 |
|---|---|---|---|---|
| `login.html` | `/login` | `views/auth/LoginView.vue` | 匿名入口；登录后按角色进入各端首页；注册成功返回登录表单 | 公共表单、密码输入、错误提示 |
| 无独立原型 | `/403` | `views/system/ForbiddenView.vue` | 路由守卫或接口 403 | 结果页 |
| 无独立原型 | `/:pathMatch(.*)*` | `views/system/NotFoundView.vue` | 未匹配地址 | 结果页 |

## 3. 学生端

F6 已实现下列页面与路由（验收见《FRONTEND-F6验收记录》）。

| HTML 原型 | Vue 路由 | 页面组件 | 入口与跨页跳转 | 端内差异 |
|---|---|---|---|---|
| `student-home.html` | `/student/home` | `views/student/StudentHomeView.vue` | 登录落点；进入工单详情；打开报修弹窗 | 展示本人进行中工单；全校工单动态区待接口 |
| `student-repair-create.html` | 无独立路由 | `views/student/RepairCreateDialog.vue` | 由首页右上角“我要报修”打开 | 四级位置级联、重复报修确认 |
| `student-orders.html` | `/student/orders` | `views/student/StudentOrdersView.vue` | 底部“我的工单”；进入详情；可取消工单 | 只查询本人发起工单 |
| `student-order-detail.html` | `/student/orders/:id` | `views/student/StudentOrderDetailView.vue` | 从首页或我的工单进入；进入确认评价 | 显示取消、确认或返工入口 |
| `student-order-review.html` | `/student/orders/:id/review` | `views/student/StudentOrderReviewView.vue` | 从待确认工单详情进入；完成后返回详情 | 确认、评分、评价、返工 |

学生端底部导航固定为“首页、我的工单”。

## 4. 维修人员端

F7 已实现下列页面与路由（验收见《FRONTEND-F7验收记录》）。

| HTML 原型 | Vue 路由 | 页面组件 | 入口与跨页跳转 | 端内差异 |
|---|---|---|---|---|
| `worker-home.html` | `/worker/home` | `views/worker/WorkerHomeView.vue` | 登录落点；进入本人、待接单、异常工单详情 | 四区工作台；全校工单区接口待后端提供 |
| `worker-orders.html` | `/worker/orders` | `views/worker/WorkerOrdersView.vue` | 底部“我的工单”；进入详情 | 只查询本人负责工单，六状态筛选 |
| `worker-order-detail.html` | `/worker/orders/:id` | `views/worker/WorkerOrderDetailView.vue` | 从各列表进入；进入处理、材料、完成页 | 接单/中断/恢复/提交结果按状态与权限展示；申请转派禁用 + 提示 |
| `worker-process.html` | `/worker/orders/:id/process` | `views/worker/WorkerProcessView.vue` | 详情进入；保存后返回详情 | 维修过程记录；图片上传接口待提供 |
| `worker-materials.html` | `/worker/orders/:id/materials` | `views/worker/WorkerMaterialsView.vue` | 详情进入；保存后返回详情 | 材料明细维护 |
| `worker-complete.html` | `/worker/orders/:id/complete` | `views/worker/WorkerCompleteView.vue` | 详情进入；成功后返回详情 | 完成说明；图片上传接口待提供 |
| `worker-leave.html` | `/worker/leave` | `views/worker/WorkerLeaveView.vue` | 底部“请假” | 请假接口待后端提供，空态提示 + 发起按钮禁用 |
| `worker-messages.html` | `/worker/messages` | `views/worker/WorkerMessagesView.vue` | 顶栏信封进入 | 消息接口待后端提供，空态提示；信封入口常驻 |

## 5. 管理员端

F8 已实现下列页面与路由（验收见《FRONTEND-F8验收记录》）。

| HTML 原型 | Vue 路由 | 页面组件 | 入口与跨页跳转 | 公共/差异能力 |
|---|---|---|---|---|
| `admin-dashboard.html` | `/admin/dashboard` | `views/admin/AdminDashboardView.vue` | 登录落点；统计卡片进入对应管理页 | 四张统计卡数字“—”（统计接口待提供）；待审批卡片可展开；今日优先事项取异常工单前 5 条 |
| `admin-workers.html` | `/admin/config/workers` | `views/admin/config/WorkerManagementView.vue` | 基础维护导航 | 人员、账号、技能、区域范围 |
| `admin-regions.html` | `/admin/config/areas` | `views/admin/config/AreaManagementView.vue` | 基础维护导航 | 四级树、浏览/编辑模式 |
| `admin-schedules.html` | `/admin/config/schedules` | `views/admin/config/WorkScheduleView.vue` | 基础维护导航 | 方案新增、编辑、启停 |
| `admin-fault-types.html` | `/admin/config/fault-types` | `views/admin/config/FaultTypeView.vue` | 基础维护导航 | 故障类型维护 |
| `admin-orders.html` | `/admin/orders` | `views/admin/AdminOrdersView.vue` | 工单管理导航；整行进入详情 | 校区、负责人、状态组合筛选 |
| `admin-order-detail.html` | `/admin/orders/:id` | `views/admin/AdminOrderDetailView.vue` | 从任一管理员工单列表进入 | 全量详情、维修轨迹与维修记录，只读 |
| `admin-exceptions.html` | `/admin/orders/exceptions` | `views/admin/AdminExceptionsView.vue` | 工单管理导航或工作台统计 | 全部异常/即将超时筛选，支持 `?type=timeout` 直达 |
| `admin-transfer-approvals.html` | `/admin/approvals/transfers` | `views/admin/AdminTransferApprovalsView.vue` | 工单管理导航或工作台待审批 | 转派审批接口待后端提供，空态提示 |
| `admin-dispatch.html` | `/admin/orders/manual-dispatch` | `views/admin/AdminManualDispatchView.vue` | 工单管理导航或工作台统计 | 人工派单接口待后端提供，空态提示 |
| `admin-leave-approvals.html` | `/admin/approvals/leaves` | `views/admin/AdminLeaveApprovalsView.vue` | 工单管理导航或工作台待审批 | 请假审批接口待后端提供，空态提示 |
| `admin-settings.html` | 首期不建立 | 无 | 已由四个基础维护页面替代 | 原型遗留页面 |

## 6. 公共组件

F3 已实现下列组件，详细 props、事件与使用约定见 `前端公共组件清单与使用约定.md`。F4 新增 `PaginationBar`（分页条）、`RegionTreeNode`（四级区域树递归节点）与 `WorkerCard`（维修人员卡片）。

| 组件 | 文件 | 使用页面 |
|---|---|---|
| `OrderStatusTag` | `components/common/OrderStatusTag.vue` | 三端全部工单列表、详情、工作台 |
| `OrderCard` | `components/common/OrderCard.vue` | 学生端和维修人员端列表、工作台 |
| `OrderTableRow` | `components/common/OrderTableRow.vue` | 管理员工单列表、异常和派单页面 |
| `OrderTimeline` | `components/common/OrderTimeline.vue` | 三端工单详情 |
| `PageState` | `components/common/PageState.vue` | 所有远程数据页面的加载、空数据、错误状态 |
| `FilterBar` | `components/common/FilterBar.vue` | 三端工单列表及管理员维护列表 |
| `ConfirmDialog` | `components/common/ConfirmDialog.vue` | 取消、删除、启停、审批、派单等操作 |
| `FormDialog` | `components/common/FormDialog.vue` | 新增、编辑弹窗 |
| `ImageUploader` | `components/common/ImageUploader.vue` | 学生报修、维修记录、完成维修（上传接口阻塞） |
| `PaginationBar` | `components/common/PaginationBar.vue` | F4 四个基础配置页分页 |
| `RegionTreeNode` | `components/admin/RegionTreeNode.vue` | 区域配置四级树 |
| `WorkerCard` | `components/admin/WorkerCard.vue` | 维修人员管理列表 |
| `MobileAppShell` | `layouts/MobileAppShell.vue` | 学生端、维修人员端 |
| `AdminAppShell` | `layouts/AdminAppShell.vue` | 管理员端 |

## 7. 验收口径

- 表中每个纳入范围的 HTML 原型都有且只有一个明确的 Vue 承载页面或弹窗。
- 列表到详情均通过整卡片或整行点击进入，内部操作按钮必须阻止冒泡。
- 三端详情复用同一生命周期展示语义，但按角色使用独立接口和操作权限。
- 所有业务数据来自 API；加载失败或无数据时使用状态组件，不回退到演示数据。
