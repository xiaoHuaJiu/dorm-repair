# FRONTEND F7 验收记录（2026-09-24）

## 验收范围

维修人员端 12 个子项（`FRONTEND-PLAN.md` F7 节）：

- F7-01 公共框架：移动端布局壳复用，顶栏消息信封入口与未读红点。
- F7-02 工作台：维修中、待接单、异常、全校工单四个业务区。
- F7-03 我的工单：六状态筛选列表。
- F7-04 工单详情：基本信息、维修轨迹与操作入口。
- F7-05 接单：预计完成时间（可选）弹窗提交。
- F7-06 过程记录：内容必填提交。
- F7-07 材料登记：材料表单与已用材料列表。
- F7-08 中断与恢复：中断原因四枚举 + 恢复直调。
- F7-09 转派申请：接口缺失，按钮禁用 + title 提示。
- F7-10 完成维修：完成说明提交。
- F7-11 请假：接口缺失，空态提示 + 发起按钮禁用。
- F7-12 消息中心：接口缺失，空态提示；顶栏信封入口常驻。

## 交付物

- `frontend/src/views/worker/WorkerHomeView.vue`（重写：四区工作台）
- `frontend/src/views/worker/WorkerOrdersView.vue`（新建）
- `frontend/src/views/worker/WorkerOrderDetailView.vue`（新建）
- `frontend/src/views/worker/WorkerProcessView.vue`、`WorkerMaterialsView.vue`、`WorkerCompleteView.vue`、`WorkerLeaveView.vue`、`WorkerMessagesView.vue`（新建）
- `frontend/src/router/index.ts`（worker 域 8 条路由）
- `frontend/src/layouts/MobileAppShell.vue`（role=2 常驻消息入口）
- `frontend/src/styles/main.css`（消息入口样式）
- `frontend/src/types/order.ts`、`api/order.ts`（六个维修操作请求/接口）
- `frontend/src/constants/order.ts`（中断原因枚举、工作台筛选状态）
- 8 个页面测试 spec（`views/worker/*.spec.ts`）
- 文档：`doc/api/前端联调接口说明.md`（3.3 节）、`doc/api/前端页面接口依赖矩阵.md`（第 4 节）、`doc/architecture/前端页面与路由映射.md`（第 4 节）

## 自动化验证

在 `frontend` 目录执行：

```text
node .\node_modules\vitest\vitest.mjs run --root . src/views/worker
Test Files  8 passed (8)
Tests       34 passed (34)

node .\node_modules\vitest\vitest.mjs run --root .   （全量）
Test Files  46 passed (46)
Tests       254 passed (254)

node .\node_modules\vue-tsc\bin\vue-tsc.js -b
exit code 0

node .\node_modules\vite\bin\vite.js build
built in 10.97s，exit code 0
```

测试覆盖：工作台三区独立加载与重试、全校工单区提示、六状态筛选、详情四态、接单/中断/恢复提交与刷新、中断内容必填校验、转派禁用提示、完成维修与材料登记表单、请假与消息空态、顶栏消息入口。

## 原型对照

- 工作台四区布局按原型 `worker-home.html`（左 1.6fr 主区 + 右 330px 侧区，900px 断点堆叠）；全校工单区原型为动态列表，因接口缺失改为静态“接口待后端提供”提示。
- 详情页按原型 split 布局：主区基本信息 + 维修轨迹，右侧记录与结果三入口；操作按钮严格按状态与权限显隐，与原型按钮配置一致。
- 中断弹窗原因四枚举（等待材料/第三方介入/现场条件限制/其他）与《前端联调接口说明》10.5 节一致。
- 请假页“发起请假申请”按钮与消息中心信封入口在原型各维修端页面均有，已统一由壳层常驻。

## 已知事项（阻塞项登记）

- 转派申请接口未提供：详情“申请转派”按钮禁用 + title 提示“后端转派申请接口待提供”。
- 请假接口未提供：请假页空态提示，发起按钮禁用。
- 消息接口未提供：消息中心空态提示，未读数无法展示。
- 全校工单接口未提供：维修端分页被服务端强制限定本人，工作台第四区静态提示。
- 图片上传接口未提供：过程记录与完成维修的图片选择禁用。
- 维修端详情接口只返回本人负责工单，非负责人访问返回 403，与“全校工单只读”原型口径不一致，待后端补充全校只读契约。
