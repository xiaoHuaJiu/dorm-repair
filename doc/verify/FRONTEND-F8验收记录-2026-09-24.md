# FRONTEND F8 验收记录（2026-09-24）

## 验收范围

管理员端 8 个子项（`FRONTEND-PLAN.md` F8 节）：

- F8-01 公共框架：顶栏 + 左侧分组导航 + 高亮 + 面包屑（沿用 AdminAppShell 壳）。
- F8-02 工作台：四张统计卡片 + 待审批展开抽屉（展开时其余卡片收缩）+ 今日优先事项。
- F8-03 全部工单：校区/负责人/状态组合筛选 + 整行进入详情。
- F8-04 管理员工单详情：基本信息、负责人、维修轨迹与维修记录（只读）。
- F8-05 异常工单：全部异常/即将超时筛选，支持 `?type=timeout` 直达。
- F8-06 转派审批：接口缺失，空态提示。
- F8-07 待人工派单：接口缺失，空态提示。
- F8-08 请假审批：接口缺失，空态提示。

## 交付物

- `frontend/src/views/admin/AdminDashboardView.vue`（重写：统计卡 + 审批抽屉 + 优先事项）
- `frontend/src/views/admin/AdminOrdersView.vue`、`AdminExceptionsView.vue`、`AdminOrderDetailView.vue`（新建）
- `frontend/src/views/admin/AdminTransferApprovalsView.vue`、`AdminManualDispatchView.vue`、`AdminLeaveApprovalsView.vue`（新建，接口缺失空态页）
- `frontend/src/router/index.ts`（admin 域 6 条路由）
- `frontend/src/types/order.ts`（`OrderQuery.workerId` 补充）
- `frontend/src/layouts/AdminAppShell.vue`（新增面包屑：导航组 / 页面标题）、`AdminAppShell.spec.ts`（侧栏分组与面包屑断言更新）
- `frontend/src/styles/main.css`（面包屑样式）
- 7 个页面测试 spec（`views/admin/*.spec.ts`）
- 文档：`doc/api/前端联调接口说明.md`（3.4 节）、`doc/api/前端页面接口依赖矩阵.md`（第 6 节）、`doc/architecture/前端页面与路由映射.md`（第 5 节）

## 自动化验证

在 `frontend` 目录执行：

```text
node .\node_modules\vitest\vitest.mjs run --root . src/views/admin
Test Files  9 passed (9)
Tests       58 passed (58)

node .\node_modules\vitest\vitest.mjs run --root .   （全量）
Test Files  46 passed (46)
Tests       254 passed (254)

node .\node_modules\vue-tsc\bin\vue-tsc.js -b
exit code 0

node .\node_modules\vite\bin\vite.js build
built in 10.97s，exit code 0
```

测试覆盖：四张统计卡与“—”占位、待审批抽屉展开/收起（aria-expanded）、今日优先事项按 `exceptionFlag` 查询、组合筛选参数（`campusId/workerId/statusList`）、异常页 `exceptionFlag/acceptTimeout` 切换与 `?type=timeout` 直达、详情并行加载与负责人姓名映射、负责人列表加载失败不阻塞详情、错误重试、空态返回、三个审批页接口缺失提示。

## 原型对照

- 工作台四卡布局按原型 `admin-dashboard.html`（四列 grid，展开待审批时 `2.6fr 0.7fr 0.7fr 0.7fr` 收缩布局，900px 断点两列）；待审批抽屉内转派审批与请假审批两组，接口缺失以提示替代列表。
- 全部工单筛选按原型三字段（区域、负责人、状态），整行点击进入详情。
- 异常工单按原型支持全部异常/即将超时两类入口，工作台“超时风险”卡以 `?type=timeout` 直达即将超时列表。
- 管理员工单详情按原型展示位置、当前负责人、联系人、报修与截止时间、问题描述、维修轨迹与维修记录；原型无操作按钮，保持只读。

## 已知事项（阻塞项登记）

- 工作台统计接口未提供：待审批、待人工派单、超时风险、异常数量四个数字统一“—”，待后端提供统计接口后替换。
- 转派审批接口未提供：列表、通过、驳回均缺失，页面空态提示。
- 人工派单接口未提供：待派单列表、候选人员与确认派单缺失，页面空态提示。
- 请假审批接口未提供：列表、通过、驳回均缺失，页面空态提示。
- 今日优先事项以“异常工单前 5 条”作为接口缺失期间的近似实现，后端提供工作台小列表接口后替换。
