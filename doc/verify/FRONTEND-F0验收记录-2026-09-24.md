# FRONTEND F0 验收记录（2026-09-24）

## 验收范围

- F0-01：原型页面、Vue 路由、页面组件、入口和跨页跳转映射。
- F0-02：逐页查询/提交接口、接口状态和后端阻塞项矩阵。

## 交付物

- `doc/architecture/前端页面与路由映射.md`
- `doc/api/前端页面接口依赖矩阵.md`
- `doc/api/README.md` 中的矩阵入口

## 验证结果

1. 扫描 `prototype/*.html`，排除统一入口 `index.html` 和已确认不纳入首期的 `admin-settings.html` 后，共有 25 个原型页面。
2. 使用脚本逐一检查文件名是否出现在页面映射文档中，结果为 `Expected=25 Missing=0`。
3. 页面映射覆盖登录、学生端、维修人员端和管理员端，并明确了提交报修弹窗、三个端的详情入口及管理员遗留页面处理方式。
4. 接口矩阵为所有远程数据区域标记了“已具备、待联调、缺失/阻塞”，缺失接口没有对应的前端演示数据方案。
5. 已记录分页集合字段 `records/list` 不一致、工单状态数值缺失、全校工单、图片上传和各业务命令接口等并行开发阻塞项。

## 未完成内容

- 本阶段不实现任何 Vue 业务页面和业务接口调用。
- 阻塞接口由后端并行开发，前端不会在接口确认前自行编造字段或枚举。
