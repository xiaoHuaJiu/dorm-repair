# 学生工单操作权限

确认、评价与返工接口同时执行角色权限和行级权限校验。学生 ID 只从 `UserContext` 获取；服务端锁定工单后比较 `repair_order.student_uid`，禁止通过请求参数冒用身份。

不存在返回 404，存在但不属于当前学生返回 403。确认和返工使用带学生 ID、原状态条件的 CAS；评价由工单行锁、前置检查和 `uk_evaluation_order` 唯一索引共同保证一次性。
