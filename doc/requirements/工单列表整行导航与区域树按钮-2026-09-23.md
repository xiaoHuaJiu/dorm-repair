# 工单列表整行导航与区域树按钮

## 区域配置

- 区域树每个节点右侧使用圆形 `+` 按钮发起新增子节点操作。
- 按钮保留“在当前节点下新增子节点”的可访问名称和悬停提示。

## 工单列表导航

- 学生端、维修人员端和管理员端的工单列表不再显示“查看详情”类按钮。
- 点击工单卡片或表格行的任意非操作区域，进入该角色对应的工单详情页。
- 工单行支持键盘聚焦，并可通过回车键或空格键进入详情。
- 行内取消工单等独立业务按钮保留；点击独立按钮不得触发详情跳转。
- 管理端工单表格移除“操作”列。
